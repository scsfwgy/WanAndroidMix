<resources>


    <style name="AppTheme.windowIsTranslucent" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- 侧滑删除相关设置 -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>


        <!-- Customize your theme here. -->
        <!--标题栏-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--状态栏-->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--系统中，子空间，复选框，单选按钮，选中时的颜色-->
        <item name="colorAccent">@color/colorAccent</item>

        <!--窗体的背景色-->
        <item name="android:windowBackground">@color/color_global_bg_container</item>
        <!--窗体无标题栏-->
        <item name="windowNoTitle">true</item>
        <!--无actionbar-->
        <item name="windowActionBar">true</item>
        <!--取消状态栏,也就是全屏显示-->
        <item name="android:windowFullscreen">false</item>

        <!--在这里可以做很多事，比如全局光标的颜色、按钮按下的反馈等等-->

    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="pmbb_ib_tabimg">
        <item name="android:layout_width">30dp</item>
        <item name="android:layout_height">30dp</item>
        <item name="android:background">#0000</item>
        <item name="android:clickable">false</item>
        <item name="android:scaleType">fitXY</item>
    </style>

    <style name="pmbb_ll_tabContainer">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:descendantFocusability">beforeDescendants</item>
        <item name="android:gravity">center</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="pmbb_tv_tabtv">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">2dp</item>
        <item name="android:textColor">@color/color_global_text_second</item>
        <item name="android:textSize">@dimen/dimens_global_text_bottombar</item>
    </style>

</resources>
