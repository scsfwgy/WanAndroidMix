<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.gy.wanandroidmix"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.windowIsTranslucent"
        tools:replace="icon,label,theme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.DealActivity">
        </activity>
        <activity android:name=".ui.activity.ProflieActivity"/>
        <activity android:name=".ui.activity.PieChartActivity">
        </activity>
        <activity android:name=".ui.activity.RadarChartActivity">
        </activity>
        <activity android:name=".ui.activity.RadarChart2Activity">
        </activity>
        <activity android:name=".ui.activity.SearchActivity">
        </activity>
        <activity android:name=".ui.activity.SonTreeActivity">
        </activity>
        <activity android:name=".ui.activity.TestActivity">
        </activity>
    </application>

</manifest>